<UserControl x:Class="TeamTaskManager.Views.WikiMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             Background="#F3F4F6" Height="600" Width="960">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <HierarchicalDataTemplate x:Key="ArticleTreeTemplate" ItemsSource="{Binding SubArticles}">
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="⭐ " Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVis}}" Foreground="#F59E0B" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock Text="[Szkic] " Visibility="{Binding IsDraft, Converter={StaticResource BoolToVis}}" Foreground="#9CA3AF" FontSize="11" VerticalAlignment="Center" FontStyle="Italic"/>
                <TextBlock Text="{Binding Title}" FontSize="14" Foreground="#374151" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="12" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Baza wiedzy" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <Border Grid.Row="1" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Height="35" Margin="0,0,0,10">
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0" FontSize="13"/>
                </Border>

                <TreeView Grid.Row="2" ItemsSource="{Binding RootArticles}" ItemTemplate="{StaticResource ArticleTreeTemplate}" 
                          BorderThickness="0" Background="Transparent" SelectedItemChanged="TreeView_SelectedItemChanged"/>

                <Button Grid.Row="3" Content="+ Nowa strona" Command="{Binding CreateNewArticleCommand}" 
                        Background="#2563EB" Foreground="White" BorderThickness="0" Height="40" FontWeight="SemiBold" Margin="0,10,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="25" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <ItemsControl ItemsSource="{Binding Breadcrumbs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Title}" Foreground="#6B7280" FontSize="12" Cursor="Hand" TextDecorations="Underline"/>
                                        <TextBlock Text=" > " Foreground="#9CA3AF" FontSize="12" Margin="4,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="{Binding SelectedArticle.Title}" FontSize="28" FontWeight="Medium" Foreground="#1F2937" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Content="Ulubione" Command="{Binding ToggleFavoriteCommand}" Width="70" Height="30" Margin="0,0,5,0" Background="#FDF6B2" BorderThickness="0"/>
                        <Button Content="PDF" Click="BtnExportPdf_Click" Width="50" Height="30" Margin="0,0,5,0" Background="#E5E7EB" BorderThickness="0" ToolTip="Eksportuj lub drukuj"/>
                        <Button Content="Edytuj" Command="{Binding EditArticleCommand}" Width="60" Height="30" Margin="0,0,5,0" Background="#E5E7EB" BorderThickness="0"/>
                        <Button Content="Usuń" Command="{Binding DeleteArticleCommand}" Width="60" Height="30" Background="#FEE2E2" Foreground="#DC2626" BorderThickness="0"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,20">
                    <TextBlock Text="Ostatnia edycja: " Foreground="#9CA3AF" FontSize="12"/>
                    <TextBlock Text="{Binding SelectedArticle.UpdatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Foreground="#6B7280" FontSize="12" FontWeight="SemiBold" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding SelectedArticleTags}" Foreground="#10B981" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <md:MarkdownViewer x:Name="WikiMarkdownViewer" Markdown="{Binding SelectedArticle.Content}" Margin="0"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="15" Width="220" Visibility="{Binding HasToc, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Spis treści" FontSize="16" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,10"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TableOfContents}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}" Margin="{Binding Margin}" FontSize="13" Foreground="#4B5563" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>