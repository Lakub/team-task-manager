<UserControl x:Class="TeamTaskManager.Views.WorklogFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:enums="clr-namespace:TeamTaskManager.Models.Enums">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Spędzony czas *" FontSize="14" Foreground="Gray"/>
                <TextBlock Grid.Column="2" Text="(np. 3w 4d 12h 30m)" FontSize="12" VerticalAlignment="Center" Foreground="Gray"/>
            </Grid>
            <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="8" Height="40">
                <TextBox Text="{Binding TimeSpentInput, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                            Validation.ErrorTemplate="{StaticResource ErrorTemplate}" Background="Transparent"
                            BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="16"/>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Data rozpoczęcia *" FontSize="14" Foreground="Gray" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" BorderBrush="#CCC" BorderThickness="1" CornerRadius="8" Height="40" Margin="0,0,5,0">
                    <DatePicker x:Name="StartDatePicker" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                Validation.ErrorTemplate="{StaticResource SilentErrorTemplate}" Background="Transparent" BorderThickness="0"
                                VerticalContentAlignment="Center" FontSize="16" Padding="5,0"/>
                </Border>
                <Border BorderBrush="#CCC" Grid.Column="1" BorderThickness="1" CornerRadius="8" Height="40" Margin="0,0,5,0">
                    <TextBox Text="{Binding StartHour, UpdateSourceTrigger=LostFocus}" Background="Transparent" Width="40" 
                             BorderThickness="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="10,0" FontSize="16"/>
                </Border>
                <TextBlock Grid.Column="2" Text=":" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Border BorderBrush="#CCC" Grid.Column="3" BorderThickness="1" CornerRadius="8" Height="40" Margin="0,0,5,0">
                    <TextBox Text="{Binding StartMinute, UpdateSourceTrigger=LostFocus}" Background="Transparent" Width="40" 
                             BorderThickness="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="10,0" FontSize="16"/>
                </Border>
            </Grid>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=StartDatePicker}"
               Foreground="Red" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"
               Visibility="{Binding (Validation.HasError), ElementName=StartDatePicker,
                            Converter={StaticResource BoolToVisibilityConverter}}"/>

        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Opis pracy" FontSize="14" Foreground="Gray" Margin="0,0,0,5"/>
            <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" CornerRadius="8">
                <TextBox Background="Transparent" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True" Padding="10" FontSize="14"
                         VerticalScrollBarVisibility="Auto" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
            </Border>
        </Grid>
        
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</UserControl>