<UserControl x:Class="TeamTaskManager.Views.TaskDetailsWindowOld"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F3F3F3">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedTask.Title, FallbackValue='No title'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Task Description"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <TextBox Text="{Binding SelectedTask.Description, FallbackValue='No description'}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="90"
                             Padding="8"
                             Background="#FAFAFA"
                             BorderBrush="#D0D0D0"
                             BorderThickness="1"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Task Status"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <StackPanel Orientation="Horizontal">
                        <ComboBox Width="180"
                                  ItemsSource="{Binding AvailableStatuses}"
                                  SelectedItem="{Binding SelectedStatus}"
                                  Margin="0,0,10,0"/>
                        <Button Content="Save Status"
                                Click="SaveStatus_Click"
                                Padding="14,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Add Comment"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Comment Text"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <TextBox x:Name="CommentInputBox"
                             Text="{Binding NewCommentText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             MinHeight="100"
                             Padding="8"
                             BorderBrush="#D0D0D0"
                             BorderThickness="1"
                             Margin="0,0,0,10"/>

                    <Button Content="Add Comment"
                            Click="AddComment_Click"
                            Width="150"
                            Padding="14,6"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Log Work Time"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Work Description"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <TextBox Text="{Binding NewWorklogDescription, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="70"
                             Padding="8"
                             BorderBrush="#D0D0D0"
                             BorderThickness="1"
                             Margin="0,0,0,10"/>

                    <TextBlock Text="Minutes"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <TextBox Width="120"
                             Text="{Binding NewWorklogMinutes, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10"/>

                    <Button Content="Add Time"
                            Click="AddWorklog_Click"
                            Width="120"
                            Padding="14,6"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Comments"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,10"/>

                        <ItemsControl ItemsSource="{Binding Comments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F8F8"
                                            BorderBrush="#DADADA"
                                            BorderThickness="1"
                                            Padding="10"
                                            Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Commenter.FullName, FallbackValue='User'}"
                                                       FontWeight="Bold"/>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                                       FontSize="12"
                                                       Foreground="Gray"
                                                       Margin="0,3,0,6"/>
                                            <TextBlock Text="{Binding Text}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Time Entries"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,10"/>

                        <ItemsControl ItemsSource="{Binding Worklogs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F8F8"
                                            BorderBrush="#DADADA"
                                            BorderThickness="1"
                                            Padding="10"
                                            Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding User.FullName, FallbackValue='User'}"
                                                       FontWeight="Bold"/>
                                            <TextBlock Text="{Binding LoggedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                                       FontSize="12"
                                                       Foreground="Gray"
                                                       Margin="0,3,0,6"/>
                                            <TextBlock Text="{Binding Description, FallbackValue='No description'}"
                                                       TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding TimeSpent, StringFormat='Time: {0}'}"
                                                       FontWeight="SemiBold"
                                                       Margin="0,6,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
