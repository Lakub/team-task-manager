<UserControl x:Class="TeamTaskManager.Views.WikiMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             Background="#F3F4F6" Height="600" Width="960">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="12" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Baza wiedzy" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <Border Grid.Row="1" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Height="35" Margin="0,0,0,10">
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0" FontSize="13" Foreground="#1F2937" />
                </Border>

                <ItemsControl Grid.Row="2" ItemsSource="{Binding AvailableTags}" Margin="0,0,0,10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" 
                                    Command="{Binding DataContext.FilterByTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}"
                                    Background="#E5E7EB" Foreground="#374151" BorderThickness="0" 
                                    Padding="10,4" Margin="0,0,6,6" Cursor="Hand" FontSize="11" FontWeight="Medium"
                                    ToolTip="Filtruj po tym tagu">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ListBox Grid.Row="3" ItemsSource="{Binding Articles}" SelectedItem="{Binding SelectedArticle}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" FontSize="14" Foreground="#4B5563" Padding="5" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="4" Content="+ Nowa strona" Command="{Binding CreateNewArticleCommand}" Height="40" Background="{StaticResource DefaultButtonBackground}" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Margin="0,10,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="25">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding SelectedArticle.Title}" FontSize="28" FontWeight="Medium" Foreground="#1F2937" TextWrapping="Wrap"/>

                    <Button Grid.Column="1" Content="Edytuj" Command="{Binding EditArticleCommand}" Width="80" Height="35" Background="#E5E7EB" Foreground="#1F2937" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2" Content="Usuń" Command="{Binding DeleteArticleCommand}" Width="80" Height="35" Background="#FEE2E2" Foreground="#DC2626" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
                    <TextBlock Text="Ostatnia edycja:" Foreground="#9CA3AF" FontSize="12" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding SelectedArticle.UpdatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Foreground="#6B7280" FontSize="12" FontWeight="SemiBold" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding SelectedArticleTags}" Foreground="#10B981" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <md:MarkdownViewer Markdown="{Binding SelectedArticle.Content}" Margin="0,0,0,0" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>